/*
 * RequestManager Messages
 *
 * This contains all the text for the RequestManager component.
 */
import { defineMessages } from 'react-intl';

export default defineMessages({
  header: {
    id: 'app.containers.RequestManager.header',
    defaultMessage: 'This is RequestManager container !',
  },
});
